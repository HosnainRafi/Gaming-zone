# Server
NODE_ENV=development
PORT=4000

# Database (PostgreSQL example)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gaming_zone?schema=public"

# Auth
JWT_SECRET="change-me-to-a-long-random-secret"

# Optional: comma-separated allowed origins (or omit to allow all in dev)
# CORS_ORIGIN="http://localhost:5173"
